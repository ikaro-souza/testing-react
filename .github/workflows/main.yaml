name: Push to main

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Build application
        run: yarn build

  test:
    runs-on: ubuntu-latest

    steps:
      - name: Run locally
        run: 'cd cypress-course && yarn cypress run-ct'
  # publish
